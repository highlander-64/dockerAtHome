[Unit]
Description=Nightly job - {{ backup.systemd_name }}

[Service]
Type=oneshot
User=root
Group=root
ExecStart={{ dockerall.path }}/{{ backup.backup_filename }}
WorkingDirectory={{ dockerall.path }}
StandardOutput=journal
StandardError=journal